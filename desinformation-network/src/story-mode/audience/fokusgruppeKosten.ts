/**
 * Kosten der Zielgruppen-Analyse (Fokusgruppe beauftragen): Budget + eine Phase.
 * Eigenes Mini-Modul, seit der alte Appell-Vortest (FokusgruppePreTest) archiviert
 * ist und die Konstante nicht mehr aus einer Komponente gezogen werden soll.
 */
export const FOKUSGRUPPE_COST = 8;
