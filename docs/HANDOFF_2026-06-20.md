# 🤝 Handoff — Operation Westunion (Stand 2026-06-20, Abend)

**Für die nächste Session.** Kurz, ehrlich, handlungsorientiert. Lies in dieser Reihenfolge:
`SOUL.md` (Vision + Arbeitsvertrag) → `docs/STATUS.md` (Bau-Stand) →
`docs/GESAMTPLAN_2026-06-20.md` (Voll-Plan/Roadmap) → **dieses Dokument** (Was als Nächstes).

---

## 1. Wo wir stehen (in einem Absatz)
Das Spiel ist ein spielbares Bildungs-Planspiel über Desinformation (fiktive Westunion/Gallia,
keine realen Staatssymbole). Auf `main` steht der komplette **Story-Director-Spine** (gewichteter
Beat-Pool, 6 Entscheidungs-Beats, Narrativ-Gedächtnis), das **Herzstück** (Episoden, Gesellschafts-
werte, Aufträge, Bildungs-Report) und die **Assets/Pixel-Font** (via #88). Diese Session hat die
**Phase-B-Pacing**-Arbeit („spürbar härter") gebaut + die letzten Owner-Screenshot-Mängel gefixt.

- **Aktiver Branch:** `claude/gracious-keller-g43bu3`
- **Offene PR:** **#89 (Draft)** — Visual-Fixes + `audience_bohemien`-Regen + **Phase B Pacing (P2-17)**.
  Basis = aktueller `main` (`0a6da1f`). **Konfliktfrei, Gate grün, bereit für Owner-Preview → Merge.**
- **Gate (verifiziert):** `tsc 0` · `vitest 473` · `build` grün.
- **Deploy-Preview #89:** https://deploy-preview-89--disinfogame.netlify.app

---

## 2. Was diese Session geliefert hat (Inhalt von PR #89)
1. **Phase B — Pacing „spürbar härter" (P2-17)** — Owner-Entscheidung umgesetzt. Zwei Gegenwehr-Wellen
   in `StoryEngineAdapter.advancePhase`:
   - **Frühe Welle (Phase 6, garantiert einmalig):** kleiner Aufmerksamkeits-/Risiko-Stups + News
     „Erste Gegenwehr formiert sich" (Früh-Druck + Lehrmoment).
   - **Späte Eskalation (`oppositionPressure(phase)`):** mit der Phase wachsender Gegendruck nach
     **3,5-Jahre-Schonzeit** (`PACING_GRACE_PHASES = 42`); überwiegt spät den passiven Abbau →
     Dauer-Vorsicht/Leerlauf wird gefährlich. **Berührt nur Risiko/Aufmerksamkeit, NIE die Sieg-Achse**
     (`obj_destabilize`, R2 — durch `BalanceInvariant.test.ts` gepinnt).
   - **Sim-Beleg (`balance-sim.test.ts`, 36 Partien):** „Zeit-abgelaufen"-Fizzle **entfällt komplett**;
     vorsichtiges/zufälliges Spiel (vorher max. Risiko ~3, **nie** enttarnt) **kann jetzt auffliegen**
     (max. Risiko ~85–100). Gewinn- UND verlierbar bleibt (16 S / 20 N).
   - **Regressionstest:** `src/story-mode/__tests__/Pacing.test.ts` (3 Tests: frühe Welle deterministisch,
     Schonzeit risiko-arm, Spät-Eskalation in die Gefahr).
2. **Visuelle Owner-Fixes:** Font global größer (VT323 `size-adjust: 132%`), blauer Web-Fokus-Ring weg
   (jetzt amber `:focus-visible`), Sofa-Publikum größer (4 Figuren statt 5, `scale 2.2`).
3. **`audience_bohemien` neu generiert** (war „kopflos" — wiederkehrender Kopf-Crop). Pipeline-Template
   gegen den Crop gehärtet (Klausel „FULL head … clearly visible … leave margin above the head").
4. **Flaky-Test** `DecisionBeatFlow` deterministisch gemacht (Krisen-Singleton im Test neutralisiert).
5. **Doku nachgezogen:** STATUS · GESAMTPLAN · ORCHESTRATION_FEEDBACK (Lesson #7: Pacing am Verteilungs-
   Vorher/Nachher messen, nicht an einer Sieg-Quote).

---

## 3. Sofort startklar werden (Routine)
```bash
cd desinformation-network
npm ci            # WICHTIG im frischen Container: Lockfile pinnt TS 5.9.3 (sonst Toolchain-Drift)
npx tsc --noEmit  # Gate-Teil 1
npx vitest run    # Gate-Teil 2 (aktuell 473 grün)
npm run build     # Gate-Teil 3
```
**Gate ist PFLICHT vor jedem Push.** Branch-Regel: nur auf `claude/gracious-keller-g43bu3` entwickeln/pushen
(oder mit dem Owner einen neuen Branch klären). Push `git push -u origin <branch>`, dann Draft-PR sicherstellen.

**Pacing-Stellschrauben** (falls der Owner nach dem Preview nachjustieren will — `StoryEngineAdapter.ts`):
- `PACING_GRACE_PHASES` (akt. 42) — wie lange die Schonzeit dauert (höher = später/milder).
- `oppositionPressure()` — `risk: Math.min(5, ramp*0.10)` + `attention: Math.min(7, ramp*0.13)`
  (Steigung/Deckel = wie hart die Spät-Welle wird). Nach jeder Änderung: erst `balance-sim` Baseline
  loggen, ändern, gegenmessen (Enden-Verteilung + Max-Risiko je Strategie), `Pacing.test.ts` ggf. anpassen.

---

## 4. Gelockte Owner-Entscheidungen (nicht neu aufrollen)
- **NPC-Namen + reale Plattformnamen:** behalten (#84).
- **Avatar m/w:** gebaut + gemergt (#85→#88).
- **Beats bewegen nur andere Achsen, nicht die Sieg-Achse** (`DECISIONS_2026-06-20_BEATS.md`).
- **Pacing-Intensität ❓7:** **„spürbar härter (mehr Nervenkitzel)"** → P2-17 gebaut (diese Session).
- **Asset-Budget:** freigegeben; trotzdem **keine Generierung ohne Kostenansage** (SOUL).

---

## 5. Was als Nächstes — priorisierte Vorschläge

### A) Zuerst abschließen (klein, hoher Wert)
1. **PR #89 mergen**, sobald der Owner den Preview abgenommen hat (Pacing-Gefühl + die 3 Visual-Fixes +
   bohemien-Kopf). Danach ist Phase A/B inhaltlich rund.
2. **Pacing-Feintuning** nur falls der Owner es im Preview als zu hart/zu mild empfindet (s. §3).

### B) Phase C — Diegetik & visuelle Politur (Preview-getrieben, ohne Budget)
- **#85-Preview-Feinschliff** (vom #85-Autor selbst notiert): Skyline-Übergang an Bandgrenzen ·
  Audience-Sitzlinien-Pixelausrichtung (Sofa).
- **P2-2/3/4** Seitenleiste/Broadcast/ComboHints/Büro-Hotspots vollends diegetisch auflösen
  (Web-Sidebar-Reste → Pixel/Klemmbrett).
- **P2-7** Entkachelungs-Reste · **P2-8** Wohnzimmer-Sitzlinie · **P2-18** Strang-5-Flavor-Dialoge.

### C) Spine-Feinschliff (Code/Content, ohne Budget)
- **DayReport-Vorgriff** („Marina kündigt morgen an") — macht kommende Beats spürbar.
- **Lage-Berater-Heuristik** für Loyalitätsprobe/Nebel verfeinern (`recommendForState`).
- **Beat-Backlog** (je einer mit Gate, nach Lust): Sleeper-Asset · Allianz/Kontrollverlust ·
  False-Flag/Attribution · Ziel-Pivot (Meta).

### D) Inhalts-/Bildungstiefe (Content, teils Budget mit Ansage)
- **Fokusgruppe-Persona-Porträts (❓4)** ODER bewusst CSS akzeptieren (Owner-Entscheidung offen).
- **Vertonung** (Topic-Texte P2-16 · Beat-Texte) — ElevenLabs-Batch mit Kostenansage.

### E) Schulden/Aufräumen (P3, niedrige Prio)
- `npcs.json` Inline-Fallback-Dialoge (Umlaute + Stimme) · `TutorialOverlay`-Inhalt · Save/Load sichert
  `DialogLoader`-Zustand · Asset-Waisen verbinden/archivieren.

---

## 6. Fallen & Wissen (damit du nicht stolperst)
- **Toolchain-Drift:** frischer Container hat ggf. falsche TS-Version → immer `npm ci` zuerst.
- **Balance-Sim ist verrauscht** (`Math.random`-Seedung im Kern): exakte Sieg-Quoten taugen NICHT als
  „unverändert"-Beweis. Robuster: `BalanceInvariant.test.ts` (reine Effekt-Mathematik) + Enden-Verteilung.
- **Asset-Pipeline:** Vision-QC ist PFLICHT (Kopf-Crop ist der Wiederholungs-Fehler!). Skill
  `.claude/skills/pixel-asset-pipeline`. Chromium für Smoke: `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`.
- **Netlify-Webhooks:** zweite Site `thunderous-unicorn-6d7134` cancelt immer (redundant, harmlos);
  „canceled" mit `state: success` = Build übersprungen (docs-only) = harmlos.
- **Extern blockiert:** Pixel-Font-Vollablösung der `monospace`-Reste (Netz-Policy; VT323 ist via #85 da,
  Rest braucht ggf. Owner-Upload nach `public/fonts/`). `npm run lint` ohne ESLint-Config = bewusst aus.
- **Test-Lehre:** mehrtägige E2E-Pfade headless meiden → deterministischer Hook-Integrationstest +
  RTL-Modal + Store-Unit (s. `ORCHESTRATION_FEEDBACK.md` #2).

---

## 7. Stale PRs (Owner-Entscheidung, nicht von mir angefasst)
- **#62** (AI-Taktiken, Apr 2026) · **#61** (Story-Mode UX Redesign, Mär 2026) · **#29** (Deploy-Fixes,
  Dez 2025) — alle **vor** dem Story-Mode-only-Rework (#77+). Sehr wahrscheinlich **obsolet/closeable**;
  einzelne Ideen aus #62 (KI-Taktiken) könnten als Content-Inspiration dienen. Vor dem Schließen kurz
  beim Owner rückversichern.

---

## 8. Ein-Zeilen-Empfehlung
**Owner-Preview von #89 einholen → mergen → dann Phase C (Diegetik/Sofa-Sitzlinie) ODER Spine-Feinschliff
(DayReport-Vorgriff) — beides ohne Budget, hoher Spürbarkeits-Hebel.**
